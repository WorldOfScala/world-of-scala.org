name: PR Build

on:
    push:
        branches:
            - master
    pull_request:
        branches:
            - master

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - uses: WorldOfScala/actions/compile@master
          with:
           script: ./scripts/ci-build.sh


